package com.ligl.pages;

import com.ligl.base.pages.Constants;
import com.ligl.base.pages.ILiglPage;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

public class JobManagerPage extends LiglBaseSessionPage {
/*
    @FindBy(id= Constants.PASSWORD)
    WebElement loginPassword;

    @FindBy(xpath=Constants.SIGNIN_BUTTON)
    WebElement nextButton;

    public ILiglPage submitPassword(String password) {
        log("Submitting Password");
        loginPassword.sendKeys(password);
        nextButton.click();
        waitForPageToLoad();

        return new EntitySelectPage();

    }*/

}
